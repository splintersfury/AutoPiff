# AutoPiff Environment Variables
# Copy to .env and fill in real values:  cp .env.example .env

# MWDB API key (generate from MWDB admin panel -> API keys)
MWDB_API_KEY=

# VirusTotal API key (optional — enables VT driver polling)
VT_API_KEY=

# Telegram bot token + chat ID (optional — enables alerting)
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# Anthropic API key (required for KernelSense LLM stage)
ANTHROPIC_API_KEY=

# MinIO credentials (defaults are fine for local dev)
# MINIO_ROOT_USER=minioadmin
# MINIO_ROOT_PASSWORD=minioadmin

# MWDB credentials (defaults are fine for local dev)
# MWDB_SECRET_KEY=change-me-in-production
# MWDB_ADMIN_PASSWORD=admin
